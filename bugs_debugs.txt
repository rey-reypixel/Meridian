MERIDIAN — BUGS FOUND & FIXED DURING PHASE 1 TESTING
======================================================
Date: 2026-08-08
Context: First test pass after scaffolding Phase 1 (Foundation). Local Python
was 3.7.9, incompatible with modern deps, so testing was done via Docker
(python:3.10-slim) instead.


BUG 1 — Wrong FastAPI import name
----------------------------------
File:     app/dependencies.py
Symptom:  App would crash immediately on import/startup.
Cause:    Imported `HTTPAuthCredentials` from `fastapi.security`, but that
          class doesn't exist. The real class is `HTTPAuthorizationCredentials`.
Fix:      Changed import and type hint:
              from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
              ...
              credentials: HTTPAuthorizationCredentials = Depends(security)


BUG 2 — Async/sync DB session mismatch
----------------------------------------
Files:    app/api/routes.py, app/api/auth.py, tests/conftest.py
Symptom:  Would raise `AttributeError: 'AsyncSession' object has no attribute
          'query'` on every database-touching endpoint (/api/messages,
          /api/dashboard/summary, /api/dashboard/models, /api/requests/{id},
          /auth/callback). Even where `.commit()` exists on AsyncSession, it's
          a coroutine — calling it without `await` means the commit silently
          never happens (data loss), no crash, no warning surfaced to caller.
Cause:    Routes were injecting `get_db_session` (an async generator yielding
          `AsyncSession` from db/database.py) but all the ORM code written
          against it used synchronous SQLAlchemy patterns: `db.query(...)`,
          `db.add(...)`, `db.commit()` without await.
          Meanwhile `app/dependencies.py`'s `get_current_user` was already
          correctly using a separate *synchronous* `get_db` (backed by
          `SessionLocal`) — so the two auth/db paths were inconsistent.
Fix:      Standardized on the synchronous `get_db` dependency everywhere
          (matches how the ORM code is actually written):
            - routes.py: swapped `Depends(get_db_session)` -> `Depends(get_db)`
              for all 4 endpoints, imported `get_db` from app.dependencies.
            - auth.py: same swap for the OAuth callback route.
            - tests/conftest.py: test fixture now overrides `get_db`
              (was overriding `get_db_session`, which no longer matched any
              route dependency, so the test db swap was silently doing
              nothing before this fix).
Note:     `get_db_session` (async) is still defined in db/database.py for
          future use, just not wired into any route right now.


BUG 3 — Missing email-validator dependency
---------------------------------------------
File:     app/db/schemas.py (uses `EmailStr` from pydantic)
Symptom:  ImportError at import time: "email-validator is not installed,
          run `pip install 'pydantic[email]'`" — took down the whole app
          since schemas.py is imported by routes.py at module load.
Cause:    `pydantic.EmailStr` requires the optional `email-validator`
          package, which wasn't listed in requirements.txt.
Fix:      Added `pydantic[email]` to backend/requirements.txt.


BUG 4 — Missing numpy dependency
------------------------------------
File:     app/services/context_truncation.py
Symptom:  ModuleNotFoundError: No module named 'numpy'.
Cause:    File does `import numpy as np` (used for embedding vectors, ahead
          of full Phase 2 FastEmbed integration) but numpy was never added
          to requirements.txt.
Fix:      Added `numpy` to backend/requirements.txt.


BUG 5 (minor / code smell) — Mutable default on SQLAlchemy JSON column
--------------------------------------------------------------------------
File:     app/db/models.py
Symptom:  Not yet triggered in practice (routes.py always passes
          `optimizations_applied` explicitly), but a real landmine: SQLAlchemy
          column defaults should be callables, not shared mutable objects.
          Using a literal `[]` means every row that relies on the column
          default shares the *same* list instance.
Cause:    `optimizations_applied = Column(JSON, default=[])`
Fix:      `optimizations_applied = Column(JSON, default=list)`


ENVIRONMENT ISSUE (not a code bug, but blocked local testing)
-------------------------------------------------------------
Symptom:  `pip install -r requirements.txt` failed for fastapi, tiktoken,
          anthropic (jiter dependency) — "no matching distribution found".
Cause:    Local machine's only Python is 3.7.9 (Windows Store install).
          tiktoken and anthropic's `jiter` dependency are Rust-backed and
          no longer publish wheels for Python 3.7 (both require 3.8+, and
          the README already specifies Python 3.10+).
Resolution: Skipped fighting the local interpreter. Built and ran everything
          inside the existing Dockerfile (python:3.10-slim), which was
          already part of the Phase 1 scaffold. All 31 tests pass there.


RESULT
------
After the above fixes: 31/31 tests passed inside the Docker container
(python:3.10.20, pytest 9.1.1).
  tests/test_cost_predictor.py .... 8 passed
  tests/test_model_router.py ....... 9 passed
  tests/test_routes.py ............ 14 passed


LIVE SMOKE TEST (real server, real HTTP, not just pytest)
-----------------------------------------------------------
Rebuilt the image clean (requirements.txt now has all fixes baked in, no
manual pip installs needed), then ran the container for real with uvicorn
on port 8001 and hit it with curl:

  GET  /health                          -> 200 {"status":"healthy",...}
  GET  /                                -> 200 {"name":"Meridian",...}
  GET  /auth/login                      -> 200 returns Google OAuth URL
  GET  /api/estimate     (no token)     -> 401 (correctly rejected)
  POST /api/messages     (no token)     -> 401 (correctly rejected)
  GET  /docs                            -> 200 (Swagger UI loads)
  GET  /openapi.json                    -> 200 (schema loads)

Then created a real user row in the SQLite DB, minted a real JWT with
create_access_token(), and re-tested the protected endpoints with a live
Authorization header:

  GET /api/estimate?prompt=...&model=claude-opus  (with Bearer token)
    -> 200 {"estimated_cost":0.015165,"token_count":11,"model":"claude-opus"}
    (tiktoken counted 11 real tokens, cost math checks out against the
     $15/1M input, $75/1M output Opus pricing table)

  GET /api/dashboard/summary  (with Bearer token)
    -> 200 {"total_spend_month":0.0,...,"requests_optimized":0}
    (correct empty-state response for a brand-new user)

This confirms the JWT auth dependency chain, the DB session fix (Bug 2),
and the cost predictor are wired together correctly end-to-end, not just
passing in mocked/isolated pytest fixtures.

Container was stopped and removed after verification (`docker stop/rm
meridian-live`) to free port 8001.


======================================================
PHASE 2 — Postgres + Redis + Celery + real FastEmbed
======================================================
Date: 2026-08-08 (same day, continued)
Context: Brought forward Postgres/Redis/Celery from "later" into Phase 2
(user's call), plus implemented the real optimization engines that were
stubbed in Phase 1: FastEmbed semantic context truncation and a real
Celery+Redis batch queue (Phase 1's batch_manager was imported but never
actually called from routes.py — dead code).

FIX (proactive, found via reasoning not a failing test) — missing
HTTPException re-raise in create_message
--------------------------------------------------------------------------
File:     app/api/routes.py
Issue:    create_message's try/except had a catch-all `except Exception`
          with no `except HTTPException: raise` above it (unlike
          get_request_details, which already had this pattern correctly).
          Since Phase 2 adds deliberate `raise HTTPException(504, ...)` /
          `raise HTTPException(502, ...)` calls inside that same try block
          for the new batch-processing path, without this fix they'd have
          been caught by the generic handler and flattened into a useless
          500 "Error processing request: 504: Batch processing timed out"
          instead of a real 504/502 with a clean message.
Fix:      Added `except HTTPException: raise` before the generic handler.


CLEANUP — removed dead CacheEntry model
--------------------------------------------------------------------------
File:     app/db/models.py, app/db/__init__.py
Finding:  CacheEntry (a Postgres table for storing embeddings as JSON) was
          never actually used anywhere — Phase 1's cache.py was a plain
          in-memory dict that never touched this model. Grepped the whole
          backend/ to confirm zero references outside its own definition
          and the __init__.py re-export.
Action:   Removed it rather than migrate a dead table into Postgres, since
          Phase 2 moves embeddings to Redis anyway (the model's whole
          reason to exist).


DESIGN NOTE — batching vs. a synchronous HTTP API
--------------------------------------------------------------------------
Anthropic's real Message Batches API has ~24h turnaround, which doesn't
fit a request/response HTTP endpoint. Implemented instead: requests with
batch=true get pushed onto a Redis list keyed by (model, task_type); once
the group hits batch_size or sits past batch_timeout, a Celery task
(meridian.process_batch, in app/celery_app.py) pops the group and calls
Claude sequentially for each item in the *worker* container, storing each
result back in Redis under its own request_id. The original HTTP handler
polls (via run_in_threadpool, so it doesn't block the event loop) for its
own request_id's result.

Gap: there's no Celery beat/periodic flush (explicit Phase 2 scope
decision), so a batch with no further arrivals just sits until something
checks it. Safety net: if a request's own wait_for_result poll times out
(15s), it force-flushes its batch inline rather than hanging forever or
erroring for no reason. Verified both paths actually happen (see live
tests below) — a 2-request batch with batch_size=2 flushed for real via
Celery .delay() and showed up in the worker's logs; a lone 1-request
batch (nothing to fill it) correctly fell through to the inline fallback
after its wait timed out.


TESTS
-----
48/48 pytest passed in the rebuilt Docker image (up from 31 in Phase 1):
  tests/test_batch_manager.py ......... 8 passed  (fakeredis, no live Redis needed)
  tests/test_context_truncation.py .... 9 passed  (fake embedder, no live
                                                     FastEmbed model or Redis needed)
  tests/test_cost_predictor.py ........ 8 passed
  tests/test_model_router.py .......... 9 passed
  tests/test_routes.py ................ 14 passed
All of Phase 1's tests still pass unmodified — the short-circuit in
truncate_context (len(messages) <= preserve_recent returns early) means
the existing single-message test cases never touch FastEmbed/Redis at
all, so nothing needed updating there.


LIVE STACK VERIFICATION (docker compose up postgres redis backend worker)
---------------------------------------------------------------------------
1. Alembic against real Postgres:
     alembic upgrade head    -> created users + api_requests tables
     alembic revision --autogenerate -> generated an EMPTY migration
       (upgrade()/downgrade() both just `pass`), proving the hand-written
       0001_initial migration exactly matches models.py, zero drift.
     alembic downgrade base && alembic upgrade head -> both directions
       ran clean, confirming the downgrade() I wrote by hand is correct.

2. Real FastEmbed semantic truncation (not mocked) against a live 7-message
   conversation containing two unrelated topics (capital of France vs.
   chocolate cake recipe): correctly dropped the 2 "France" messages while
   keeping all 5 cake-related ones. First call: ~17s (cold model download
   from HF Hub, no HF_TOKEN warning is expected/harmless). Second call on
   the exact same messages: 5.8ms — served entirely from the Redis
   embedding cache, in a brand-new Python process, never touched the
   FastEmbed model at all. Confirmed 5 embedding keys landed in Redis
   (meridian:embedding:*).

3. Real Celery dispatch: forced batch_size=2 in-process, sent 2 requests
   to the same (model, task_type) batch key. Second request's add_to_batch
   triggered process_batch_task.delay(...) for real. Worker logs show:
     Task meridian.process_batch[...] received
     HTTP Request: POST https://api.anthropic.com/v1/messages "401 ..."  (x2)
     Task meridian.process_batch[...] succeeded in 1.7s
   i.e. the worker container actually picked up the task, called Anthropic
   twice (sequentially, one per grouped request), got real 401s (no valid
   API key available in this environment), and completed successfully
   because process_batch_task catches per-item errors and stores them
   rather than crashing the task.

4. Live HTTP round-trip through POST /api/messages with a real Postgres
   user + JWT:
     batch:true,  1 lone request -> waited, no natural flush, inline
       fallback triggered -> 502 "Error processing batched request:
       ...401 invalid x-api-key" (clean propagation, not swallowed to 500
       — this is what the HTTPException re-raise fix above enabled)
     batch:false -> 500 "Error processing request: ...401 invalid
       x-api-key" (expected generic-error path, also clean)
     GET /api/estimate, /api/dashboard/summary, /api/dashboard/models
       -> all 200, correct empty-state / cost-math responses against the
       live Postgres user row created for this test.

No valid ANTHROPIC_API_KEY is available in this environment, so the full
happy path (an actual successful completion) couldn't be exercised end to
end — every failure above is a real 401 from Anthropic, not a bug in
Meridian's code. Everything upstream of the Anthropic call (routing,
truncation, batching, cost estimation, auth, persistence) is verified
working against real infrastructure, not mocks.

Postgres/Redis/backend/worker were left running after this test (not torn
down) in case further live testing is wanted.


======================================================
PHASE 2.5 — found while wiring up 3 previously-dead request params
======================================================
Date: 2026-08-08 (same day, continued)
Context: Before testing against a real Anthropic API key, checked whether
MessagesCreateRequest's advertised parameters (cost_limit, optimize_for,
quality_threshold) actually did anything. They didn't — 3 of 8 request
fields were accepted and silently ignored. Fixing all 3 surfaced two more
real bugs along the way.

BUG 6 — cost_limit accepted but never enforced
--------------------------------------------------------------------------
File:     app/api/routes.py (create_message)
Symptom:  A caller could pass cost_limit=0.50 ("don't spend more than
          this") and it did nothing — request always went through
          regardless of estimated cost. Directly contradicts the README's
          advertised budgeting feature.
Cause:    request.cost_limit was never read anywhere in the function.
Fix:      After computing optimized_cost (the pre-call estimate) and
          before any LLM/batch call, added:
            if request.cost_limit is not None and optimized_cost > request.cost_limit:
                raise HTTPException(402, detail=f"Estimated cost ... exceeds cost_limit ...")
          Placed before the Claude API call specifically so a request that
          would exceed budget never spends anything.
Test:     tests/test_routes.py::test_create_message_cost_limit_rejects_when_exceeded
          asserts 402 + mock_llm.assert_not_called().


BUG 7 — optimize_for accepted but never branched anything
--------------------------------------------------------------------------
File:     app/api/routes.py, app/db/schemas.py
Symptom:  optimize_for="quality" or "speed" behaved identically to the
          "cost" default — always ran the same truncation+routing
          pipeline no matter what the caller asked for. Also, the field
          was a bare `str`, so a typo like "qualtiy" silently fell through
          to default behavior instead of erroring.
Cause:    request.optimize_for was never read in create_message.
Fix:      - Context truncation now skipped when optimize_for == "speed"
            (computing embeddings adds latency, which is what "speed" mode
            is presumably asking to avoid).
          - Model routing now skipped when optimize_for == "quality" (never
            downgrade below the model the caller actually asked for).
          - Changed the schema field from `str` to
            `Literal["cost", "speed", "quality"]` so FastAPI now returns a
            422 for any other value instead of silently misinterpreting it.
Test:     test_create_message_optimize_for_quality_skips_routing,
          test_create_message_optimize_for_speed_skips_truncation,
          test_create_message_invalid_optimize_for_rejected.


BUG 8 — quality_threshold accepted but ignored (used the global default instead)
--------------------------------------------------------------------------
File:     app/api/routes.py, app/services/model_router.py
Symptom:  A caller passing a custom quality_threshold per request had no
          effect — routing always used settings.quality_threshold instead.
Cause:    ModelRouter.get_routing_decision() didn't even accept a
          quality_threshold parameter, so routes.py had nothing to pass
          through even if it wanted to.
Fix:      Added quality_threshold: Optional[float] = None to
          get_routing_decision(), forwarded to select_model(). routes.py
          now calls
            model_router.get_routing_decision(request.model, prompt_text, quality_threshold=request.quality_threshold)
Test:     test_get_routing_decision_quality_threshold_changes_model (proves
          a loose threshold picks haiku, a strict one picks opus, for the
          same prompt).


BUG 9 (found while fixing #8) — model routing accidentally gated behind
context truncation having fired
--------------------------------------------------------------------------
File:     app/api/routes.py
Symptom:  Model routing only ran `if settings.model_routing_enabled and
          len(optimizations_applied) > 0` — i.e. it silently never ran
          unless context truncation had already dropped at least one
          message. A short conversation (<= preserve_recent_messages)
          would never trigger truncation, so routing was skipped too, even
          though the two optimizations are logically independent. This
          would have made the "model routing" test scenario show nothing
          for any short prompt.
Cause:    Leftover/accidental coupling — `len(optimizations_applied) > 0`
          was presumably meant as some other condition and never revisited.
Fix:      Changed the gate to just `if settings.model_routing_enabled and
          request.optimize_for != "quality"` (see Bug 7) - routing now
          runs independently of whether truncation fired.


BUG 10 (found while fixing #8) — quality_threshold default on the wrong
scale, made routing always pick the cheapest model
--------------------------------------------------------------------------
File:     app/config.py, app/db/schemas.py
Symptom:  ModelRouter.QUALITY_SCORES are all 0-10 scale values (7.0-9.6).
          Both Settings.quality_threshold and
          MessagesCreateRequest.quality_threshold defaulted to `0.9` — a
          0-1 style number. Since every real quality score is >7x that
          threshold, EVERY task for EVERY model trivially passed the
          check, so select_model always returned the first (cheapest)
          entry in MODEL_ORDER: claude-haiku, regardless of task
          complexity. The core "intelligent routing" feature was
          non-functional by default - it always just picked Haiku.
Cause:    Default value copy-pasted from the README's example
          (`quality_threshold: float = 0.9  # Min acceptable quality`,
          written as if 0-1 normalized) without reconciling it against the
          actual 0-10 QUALITY_SCORES table used internally.
Fix:      Changed both defaults from 0.9 to 8.5 (0-10 scale). At 8.5:
          classification->haiku, summarization/coding->sonnet,
          general->sonnet - a meaningfully varied routing outcome across
          task types, instead of always Haiku.


MINOR — falsy-vs-None bug in select_model's threshold override
--------------------------------------------------------------------------
File:     app/services/model_router.py
Issue:    `quality_threshold = quality_threshold or self.quality_threshold`
          would incorrectly override an explicit `quality_threshold=0.0`
          with the instance default, since 0.0 is falsy. Same class of bug
          as Bug 5 (Phase 1) and the Phase 2 context_truncation rewrite
          already avoided. Not currently reachable with real values (0.0
          isn't a sane threshold on the 0-10 scale) but fixed for
          consistency: `quality_threshold if quality_threshold is not None
          else self.quality_threshold`.


TESTS
-----
53/53 pytest passed after these fixes (up from 48):
  + test_create_message_cost_limit_rejects_when_exceeded
  + test_create_message_optimize_for_quality_skips_routing
  + test_create_message_optimize_for_speed_skips_truncation
  + test_create_message_invalid_optimize_for_rejected
  + test_get_routing_decision_quality_threshold_changes_model


ALSO CAUGHT — .env had the same stale 0.9 value the code default just got
fixed away from
--------------------------------------------------------------------------
File:     backend/.env
Issue:    Bug 10 above fixed Settings.quality_threshold's default from 0.9
          to 8.5 in config.py, but backend/.env explicitly set
          QUALITY_THRESHOLD=0.9 - and env vars win over class defaults in
          pydantic-settings. Left as-is, the .env value would have silently
          un-done the Bug 10 fix for every real run (the code default is
          only relevant when no env var is set).
Fix:      Updated QUALITY_THRESHOLD=0.9 -> 8.5 in both backend/.env and
          backend/.env.example.


======================================================
ADDED — MOCK_ANTHROPIC dev/test toggle
======================================================
Date: 2026-08-08 (same day, continued)
Context: Wanted to verify the fixes above (and the whole Phase 2 pipeline)
against the real live HTTP server rather than just pytest, but no paid
Anthropic API key was available. Anthropic's API has no free tier for
real usage, so added a settings.mock_anthropic flag (app/config.py) that
OptimizedLLMClient._call checks (app/services/llm_client.py): when true,
skips the real anthropic.Anthropic call and returns a clearly-labeled
"[MOCK RESPONSE] ..." string instead - but still computes input/output
token counts for real via cost_predictor.count_tokens() on whatever text
the pipeline actually decided to send, so cost/truncation/routing math
stays fully honest and only the completion text itself is fake.
Not a bug fix - a deliberate, clearly-documented, off-by-default (in
.env.example) dev-only escape hatch. Set MOCK_ANTHROPIC=true in the local
backend/.env for this testing session only.

LIVE VERIFICATION (real HTTP, real Postgres/Redis/Celery, mocked LLM call,
$0 cost) - fresh test user, all 5 promised scenarios run against the
actual running server:

1. Direct call, "What is the capital of France?", requested claude-opus:
   -> routed to claude-sonnet (general task: haiku=7.5 < 8.5 threshold
      fails, sonnet=8.5 meets it). optimizations_applied=["model_routing"].
      cost $0.000306 vs original $0.003855 - confirms Bug 9 (routing no
      longer gated behind truncation) and Bug 10 (threshold scale) both
      fixed, live, not just in pytest.

2. Context truncation, the same 7-message France/cake conversation used
   in the earlier FastEmbed test: mock received "5 message(s)" (down from
   7) - matches the earlier direct-service-call result exactly.
   optimizations_applied=["context_truncation","model_routing"].

3. Model routing, classification prompt ("Classify this email as spam..."),
   requested opus:
     default (optimize_for=cost) -> routed to claude-haiku (classification:
       haiku=8.5 meets the 8.5 threshold exactly), $0.001637 saved.
     optimize_for=quality -> stayed on claude-opus, optimizations_applied
       is empty, quality_score falls back to the pre-routing default (9.0)
       since routing was skipped entirely - confirms Bug 7's fix works.

4. Batch processing, batch:true, "Summarize this: cats are great pets"
   (haiku requested): took the 15s inline-fallback path (nothing else
   arrived to fill the batch - expected, matches the documented Phase 2
   design gap re: no Celery beat). Routed haiku->sonnet (summarization
   needs 9.0, haiku only scores 8.0 there) - note this is a quality
   *upgrade*, not a downgrade, and the response correctly reports
   savings=0 rather than a negative number, since optimized_cost ended up
   higher than the original haiku-based estimate.
   optimizations_applied=["model_routing","batch_processing"].

5. Dashboard check after the above: GET /api/dashboard/summary and
   /api/dashboard/models both reflect real aggregated numbers across every
   request made this session (6 total, including a couple of earlier
   attempts) - requests_optimized=6, savings_percentage=78.86%,
   avg_quality_score=8.75, per-model breakdown correctly split across
   haiku/sonnet/opus usage counts.

MINOR OBSERVATION (not fixed, noted only) - dashboard total_cost rounds to
$0.00 per model in the aggregate breakdown when every request costs a
fraction of a cent (our test-scale amounts). avg_cost_per_request in the
same response still shows full precision, so no data is actually lost -
it's just that round(total_cost, 2) isn't granular enough to show a
sub-cent aggregate. Not worth fixing at toy test scale; real usage
volumes would accumulate past $0.01 quickly and this wouldn't be visible
in practice.


======================================================
PHASE 2.6 — Four free high-impact additions (in progress)
======================================================
Date: 2026-08-09
Context: User asked what would make the project more compelling to
companies without spending money. Landed on 4 items fully buildable AND
verifiable at $0: embedding-based task classification (replacing keyword
matching), semantic response caching, streaming responses, and real load
testing. (Real Batch API integration, LLM-as-judge quality calibration,
and multi-provider support were discussed but need paid API calls to
verify, so deferred.)

Status: DONE. Build, full test suite (68/68), and live verification
against the real running stack (Postgres/Redis/Celery, MOCK_ANTHROPIC=true)
including a real Locust load-test run all complete - see the LIVE
VERIFICATION and REAL LOAD TEST RESULTS sections below.

WHAT WAS BUILT
---------------
1. New app/services/embeddings.py - EmbeddingService, extracted out of
   context_truncation.py so the FastEmbed model + Redis cache load once
   and get shared across context truncation, task classification, and
   response caching (previously only context_truncation.py owned this;
   loading a second copy for the new consumers would've doubled memory
   and cold-start time). context_truncation.py now delegates to it.

2. app/services/model_router.py: classify_task() now embeds the prompt
   and compares it via cosine similarity against TASK_EXAMPLES (a few
   canonical prompts per category), instead of brittle keyword matching
   (`"classify" in prompt.lower()`). Falls back to "general" if even the
   best match scores below task_classification_min_confidence (0.3).

3. New app/services/response_cache.py - Redis-backed semantic cache, keyed
   per model. A near-duplicate prompt (cosine similarity >=
   response_cache_similarity_threshold, default 0.97 - deliberately high,
   since a wrong hit is worse than a miss) returns the cached response
   instead of calling the LLM. FIFO eviction past
   response_cache_max_entries (200/model), TTL 1h. Only checked/populated
   when request.temperature <= response_cache_max_temperature (0.3) -
   reusing a stale response instead of sampling a new one isn't obviously
   correct for high-temperature/creative requests, so those are excluded
   rather than pretending this is solved.

4. Streaming: app/services/llm_client.py gained stream_message() (a
   second AsyncAnthropic client for real async streaming; mock mode
   yields the same [MOCK RESPONSE] text word-by-word so streaming is also
   testable at $0). New POST /api/messages/stream endpoint (SSE), rejects
   batch=true with 400 (streaming a deferred/grouped batch result isn't a
   coherent combination without a much bigger pub/sub redesign - out of
   scope). Checks/populates the response cache same as the direct
   endpoint; on a cache hit, fake-streams the cached text in word-chunks
   instead of hitting the LLM.

5. backend/loadtest/ - locustfile.py + setup_test_user.py, ready to run
   against the live stack with MOCK_ANTHROPIC=true (free, and it's
   actually the *correct* way to isolate Meridian's own routing/
   truncation/DB overhead from Claude's response time, which is what the
   README's latency claims are really about).

BUGS / DESIGN ISSUES FOUND WHILE BUILDING THIS
------------------------------------------------
Real inefficiency fixed: classify_task() was being called up to 3x per
request (inside select_model, again in get_routing_decision, again in
routes.py's batch block) - harmless with cheap keyword matching, wasteful
once it's an embedding + N-example similarity computation. Refactored
model_router.py so select_model()/get_routing_decision() both accept an
optional pre-computed task_type, and routes.py's new
_prepare_optimized_request() helper (extracted from create_message, now
shared with the new streaming endpoint) classifies once and threads the
result through everywhere, including the batch block.

Real design question resolved: does the DB session injected via
Depends(get_db) stay valid for the full lifetime of a StreamingResponse
generator, or does FastAPI close it once the endpoint function returns
(before the stream finishes)? Rather than trust version-dependent
behavior here, create_message_stream's generator opens its own
SessionLocal() directly and closes it in a finally block, so the DB
write's lifetime is self-contained and correct regardless of FastAPI/
Starlette's exact dependency-cleanup timing for streaming responses. This
is a genuine, unresolved-in-my-head design tension, not a hypothetical -
worth flagging that it hasn't been proven safe via the alternative
(Depends(get_db)) approach, only via sidestepping the question entirely.

Test infra gap found and fixed: after wiring in embedding-based
classification, every test that goes through create_message or
create_message_stream started failing with
`redis.exceptions.ConnectionError: ... Name or service not known` -
_prepare_optimized_request now unconditionally calls classify_task()
(needs Redis for the embedding cache) and, separately, the new response
cache check also needs Redis directly. Neither is reachable in an
isolated `docker run` test invocation (no compose network). Fixed by:
mocking app.services.model_router.embedding_service.similarity with a
deterministic word-overlap fake in test_routes.py and test_streaming.py
(same pattern as test_model_router.py), and disabling response_cache.enabled
for the duration of those tests (caching behavior itself is covered
separately by test_response_cache.py, so these files don't need real
Redis at all now).

Test bug (mine, not the code's) caught by the suite doing its job: wrote
test_stream_delivers_chunks_in_order_then_done asserting
model_used == "claude-opus" for the prompt "Hello" without accounting for
the Bug 10 fix from Phase 2.5 (quality_threshold default corrected to
8.5) - "Hello" classifies as "general" (no embedding overlap with any
TASK_EXAMPLES), general/opus scores 9.0 but general/sonnet scores 8.5
which already clears the threshold, so it legitimately routes down to
sonnet - exactly the same behavior already verified live in Phase 2's
"capital of France" test. Fixed the test to pass optimize_for="quality"
so model_used is deterministic, rather than changing the (correct)
routing behavior.

TESTS
-----
68/68 passing (up from 53):
  tests/test_model_router.py - reworked for embedding-based
    classification: mocks embedding_service.similarity with a
    deterministic word-overlap function (avoids needing a real model load
    or live Redis), 5 new tests including the low-confidence fallback and
    the task_type-passthrough-skips-reclassification behavior.
  tests/test_response_cache.py (new, 8 tests) - fake embeddings + real
    fakeredis: set/get roundtrip, near-duplicate hit, dissimilar miss,
    per-model isolation, disabled-cache no-op, FIFO eviction at the cap.
  tests/test_streaming.py (new, 4 tests) - mocked llm_client.stream_message,
    verifies SSE chunk order, final metadata shape, batch+stream rejected
    with 400, and that the DB write actually lands (via the SessionLocal
    patch described above).
  tests/test_context_truncation.py - unchanged, still passes as-is (its
    per-instance get_embedding monkeypatch survives the delegation
    refactor untouched).

LIVE VERIFICATION (real Postgres/Redis/Celery, MOCK_ANTHROPIC=true, $0 cost)
-------------------------------------------------------------------------------
Date: 2026-08-09. Force-recreated backend+worker (`docker compose up -d
--force-recreate backend worker`) to pick up the latest image (the
previous containers were 2h old, predating all of Phase 2.6, and
critically didn't have `locust` installed yet). Postgres/Redis left
running unchanged.

1. Embedding-based classification, real paraphrase (not exact keyword
   match): "Please categorize this ticket by urgency level" -> correctly
   classified "classification", routed opus->haiku (8.5 score meets 8.5
   threshold), 95% cost savings. First live call took 113s - real
   FastEmbed cold-start (fresh container, model not yet cached on disk).

2. Interesting real finding, worth being honest about: "Sort this
   feedback into urgent versus non-urgent buckets" (deliberately avoids
   every old keyword) got classified as "coding" (quality_score 8.8 =
   coding/sonnet), not "classification". The word "sort" is pulling
   toward the coding example ("Write a Python function to sort a list")
   even though the sentence's actual intent is triage/categorization.
   This is a genuine limitation of a 3-4-example-per-category classifier -
   a single strong lexical anchor can dominate over whole-sentence intent.
   Not a code bug; a real, observed characteristic of the current
   TASK_EXAMPLES set worth knowing about (and a good candidate for the
   later LLM-as-judge quality-calibration work discussed but deferred, or
   just a larger/better-chosen example set).

3. Semantic response caching:
   - Exact-duplicate prompt ("Explain quantum entanglement simply" sent
     twice): second call correctly showed "semantic_cache" in
     optimizations_applied, identical cost, latency dropped 42ms -> 10ms.
   - Near-duplicate paraphrase ("...tallest mountain in the world?" vs
     "...tallest mountain on Earth?"): did NOT hit. Measured the actual
     cosine similarity directly (embedding_service.similarity(...)) =
     0.9663 against the 0.97 threshold - missed by 0.004. Confirms the
     threshold is doing real, precise work, not just a rubber-stamped
     config value; this specific paraphrase is a legitimate near-miss,
     not a broken cache.

4. Streaming: real curl -N SSE round-trip against POST
   /api/messages/stream - word-by-word "[MOCK RESPONSE] Simulated
   claude-sonnet streaming completion..." chunks arrived in order,
   followed by a correct final `done` event with real cost/routing
   metadata. Queried Postgres directly for that request_id afterward and
   confirmed the ApiRequest row was actually persisted (input_tokens=5,
   output_tokens=20, correct cost) - validates the self-contained
   SessionLocal() design decision from earlier in this phase actually
   works, not just "should work in theory."

5. GET /api/dashboard/summary after the above: requests_optimized=7,
   savings_percentage=76.84%, avg_quality_score=8.61 - correct live
   aggregation.

REAL LOAD TEST RESULTS (Locust, 20 users, spawn rate 5/s, 45s, against
the live stack with MOCK_ANTHROPIC=true - this measures Meridian's own
routing/truncation/DB overhead, not Claude's response time, which is
what the README's latency claims are actually supposed to be about)
-----------------------------------------------------------------------
Command: docker exec -e LOCUST_JWT=<token> meridian-backend-1 locust
  -f loadtest/locustfile.py --host http://localhost:8000
  --headless -u 20 -r 5 -t 45s --csv=/tmp/loadtest_results

682 total requests, 0 failures (0.00%), 15.4 req/s aggregate throughput.

Per-endpoint (median / p95 / p99, milliseconds):
  GET  /api/dashboard/summary          11 / 73 / 81
  GET  /api/estimate                    5 / 21 / 68
  POST /api/messages                   18 / 52 / 86
  POST /api/messages (long context)    20 / 35 / 76
  Aggregated                           17 / 42 / 86   (p99.9 = 200, max = 200)

Notable: "long context" requests (8 messages, exercises real context
truncation + embedding similarity lookups against Redis) show
essentially the same latency as the plain single-message endpoint
(20ms vs 18ms median) once the embedding cache is warm - the semantic
truncation pipeline isn't adding meaningful overhead once the model's
loaded and embeddings are cached, which is the whole point of caching
them in the first place.

These are real numbers from this specific machine/environment, not
representative of production hardware or network conditions - but
they're genuine, zero-failure results, not fabricated ones like the
README's current "Performance Benchmarks" table. Whether/how to update
that table with these numbers (or re-run in a more production-like
environment first) is a decision for later, not made here.

(Note: this is exactly the data that went into the README's "Performance
Benchmarks" section update on 2026-08-09 - noting it here since it wasn't
cross-referenced at the time.)


======================================================
FRONTEND/UX GAP ANALYSIS — backend data vs. the design brief
======================================================
Date: 2026-08-09
Context: Before any frontend code was written, inspected what the actual
backend returns against a detailed design brief (dark "infrastructure
console" aesthetic, specific pages: Overview, Optimizations, Requests,
Analytics, Settings). Recording this because two small backend additions
were made as a direct result (GET /api/requests, token/latency in the
summary endpoint - see the commit "Add GET /api/requests list endpoint,
aggregate tokens/latency into dashboard summary"), and because the
remaining gaps below determine what the frontend can wire to real data
today vs. what needs mock data or new backend work.

What ApiRequest actually stores per row: original_model, routed_model,
original_cost, optimized_cost, savings, optimizations_applied (list),
quality_score, input_tokens, output_tokens, latency_ms, created_at.
No prompt/message text is ever persisted.

Maps cleanly to real data today:
- Overview KPIs: total spend/saved/savings %/avg quality (no sparklines)
- Model Mix donut (usage_count -> percentage, from /api/dashboard/models)
- Requests table: id, timestamp, original/routed model, costs, savings,
  optimizations_applied, quality_score, latency (via the new
  GET /api/requests - paginated, filterable by model/date)
- Basic Request Detail (same fields as the list, single row)

Still gaps (need mock data, or new backend work, to build these):
- 7-day sparklines / "vs last 7 days" deltas / date-range picker on the
  Overview page - summary endpoint returns all-time totals only, no
  time-series aggregation exists
- Request preview text in the table ("Summarize everything we discussed...")
  - prompt text isn't stored anywhere; would be a real privacy trade-off
    to start storing it, not just an endpoint addition
- "Optimization Impact" $ breakdown by mechanism (context truncation
  contributed $X, routing contributed $Y, etc.) - not attributable with
  the current data model; savings is one combined number per request,
  and when multiple optimizations fire together there's no per-mechanism
  split. This is a modeling question, not a missing endpoint.
- Request Detail "forensic trace" (tokens before/after truncation, which
  models were evaluated and their scores, cache check similarity) - only
  the final outcome is stored, not the intermediate pipeline steps
- Task classification confidence % - computed in-memory during
  classify_task() but never persisted or returned
- Cache page (hit rate, savings) / Batching page (queue states,
  efficiency) - response_cache.py and batch_manager.py have internal
  Redis state, zero API surface exposing it
- Models analytics page (tokens/latency/quality per model, not just
  cost) - /api/dashboard/models only has usage_count/total_cost/
  avg_cost_per_request
- "Share Report" export - no backend support at all

Decision made: build the frontend in two tracks rather than block on
closing every gap - Track A wires directly to what's real today, Track B
scaffolds the rest with clearly-marked mock data until/unless the
backend gaps above get closed. Frontend implementation itself is
happening in a separate conversation, not tracked further in this file.
